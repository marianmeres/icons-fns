/**
 * Lucide `Heading` icon.
 *
 * @module
 */
import { icon, type IconFn } from "../_icon.ts";

/** Lucide `Heading` icon. */
export const iconLucideHeading: IconFn = /* @__PURE__ */ icon(16, 2, `<!-- @license lucide-static v1.33.0 - ISC -->`, ` viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" ><path d="M6 12h12" /><path d="M6 20V4" /><path d="M18 20V4" /></svg>`);
