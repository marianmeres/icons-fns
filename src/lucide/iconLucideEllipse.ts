/**
 * Lucide `Ellipse` icon.
 *
 * @module
 */
import { icon, type IconFn } from "../_icon.ts";

/** Lucide `Ellipse` icon. */
export const iconLucideEllipse: IconFn = /* @__PURE__ */ icon(16, 2, `<!-- @license lucide-static v1.33.0 - ISC -->`, ` viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" ><ellipse cx="12" cy="12" rx="10" ry="6" /></svg>`);
