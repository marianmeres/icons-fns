import { icon, type IconFn } from "../_icon.ts";

/** Lucide `Slash` icon. */
export const iconLucideSlash: IconFn = /* @__PURE__ */ icon(16, 2, `<!-- @license lucide-static v1.33.0 - ISC -->`, ` viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" ><path d="M22 2 2 22" /></svg>`);
