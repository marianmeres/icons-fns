import { icon, type IconFn } from "../_icon.ts";

/** Lucide `WifiZero` icon. */
export const iconLucideWifiZero: IconFn = /* @__PURE__ */ icon(16, 2, `<!-- @license lucide-static v1.33.0 - ISC -->`, ` viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" ><path d="M12 20h.01" /></svg>`);
