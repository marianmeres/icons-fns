/**
 * Bytesize `CaretBottom` icon.
 *
 * @module
 */
import { icon, type IconFn } from "../_icon.ts";

/** Bytesize `CaretBottom` icon. */
export const iconBytesizeCaretBottom: IconFn = /* @__PURE__ */ icon(32, 2, ``, `viewBox="0 0 32 32" fill="none" stroke="currentcolor" stroke-linecap="round" stroke-linejoin="round"><path d="M30 10 L16 26 2 10 Z" /></svg>`);
