/**
 * Bytesize `Close` icon.
 *
 * @module
 */
import { icon, type IconFn } from "../_icon.ts";

/** Bytesize `Close` icon. */
export const iconBytesizeClose: IconFn = /* @__PURE__ */ icon(32, 2, ``, `viewBox="0 0 32 32" fill="none" stroke="currentcolor" stroke-linecap="round" stroke-linejoin="round"><path d="M2 30 L30 2 M30 30 L2 2" /></svg>`);
