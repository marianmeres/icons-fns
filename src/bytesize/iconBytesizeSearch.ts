import { icon, type IconFn } from "../_icon.ts";

/** Bytesize `Search` icon. */
export const iconBytesizeSearch: IconFn = /* @__PURE__ */ icon(32, 2, ``, `viewBox="0 0 32 32" fill="none" stroke="currentcolor" stroke-linecap="round" stroke-linejoin="round"><circle cx="14" cy="14" r="12" /><path d="M23 23 L30 30" /></svg>`);
