/**
 * Bytesize `Eject` icon.
 *
 * @module
 */
import { icon, type IconFn } from "../_icon.ts";

/** Bytesize `Eject` icon. */
export const iconBytesizeEject: IconFn = /* @__PURE__ */ icon(32, 2, ``, `viewBox="0 0 32 32" fill="none" stroke="currentcolor" stroke-linecap="round" stroke-linejoin="round"><path d="M30 18 L16 5 2 18Z M2 25 L30 25" /></svg>`);
