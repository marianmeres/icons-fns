import { icon, type IconFn } from "../_icon.ts";

/** Bytesize `ChevronBottom` icon. */
export const iconBytesizeChevronBottom: IconFn = /* @__PURE__ */ icon(32, 2, ``, `viewBox="0 0 32 32" fill="none" stroke="currentcolor" stroke-linecap="round" stroke-linejoin="round"><path d="M30 12 L16 24 2 12" /></svg>`);
