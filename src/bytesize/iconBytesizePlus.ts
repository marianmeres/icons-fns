/**
 * Bytesize `Plus` icon.
 *
 * @module
 */
import { icon, type IconFn } from "../_icon.ts";

/** Bytesize `Plus` icon. */
export const iconBytesizePlus: IconFn = /* @__PURE__ */ icon(32, 2, ``, `viewBox="0 0 32 32" fill="none" stroke="currentcolor" stroke-linecap="round" stroke-linejoin="round"><path d="M16 2 L16 30 M2 16 L30 16" /></svg>`);
