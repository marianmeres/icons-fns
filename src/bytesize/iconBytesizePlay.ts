import { icon, type IconFn } from "../_icon.ts";

/** Bytesize `Play` icon. */
export const iconBytesizePlay: IconFn = /* @__PURE__ */ icon(32, 2, ``, `viewBox="0 0 32 32" fill="none" stroke="currentcolor" stroke-linecap="round" stroke-linejoin="round"><path d="M10 2 L10 30 24 16 Z" /></svg>`);
