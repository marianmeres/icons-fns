import { icon, type IconFn } from "../_icon.ts";

/** Bytesize `Send` icon. */
export const iconBytesizeSend: IconFn = /* @__PURE__ */ icon(32, 2, ``, `viewBox="0 0 32 32" fill="none" stroke="currentcolor" stroke-linecap="round" stroke-linejoin="round"><path d="M2 16 L30 2 16 30 12 20 Z M30 2 L12 20" /></svg>`);
