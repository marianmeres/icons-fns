import { icon, type IconFn } from "../_icon.ts";

/** Bytesize `Flag` icon. */
export const iconBytesizeFlag: IconFn = /* @__PURE__ */ icon(32, 2, ``, `viewBox="0 0 32 32" fill="none" stroke="currentcolor" stroke-linecap="round" stroke-linejoin="round"><path d="M6 2 L6 30 M6 6 L26 6 20 12 26 18 6 18" /></svg>`);
