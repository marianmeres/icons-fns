/**
 * Bytesize `ArrowBottom` icon.
 *
 * @module
 */
import { icon, type IconFn } from "../_icon.ts";

/** Bytesize `ArrowBottom` icon. */
export const iconBytesizeArrowBottom: IconFn = /* @__PURE__ */ icon(32, 2, ``, `viewBox="0 0 32 32" fill="none" stroke="currentcolor" stroke-linecap="round" stroke-linejoin="round"><path d="M6 22 L16 30 26 22 M16 30 L16 2" /></svg>`);
