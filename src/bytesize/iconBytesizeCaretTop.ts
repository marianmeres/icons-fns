/**
 * Bytesize `CaretTop` icon.
 *
 * @module
 */
import { icon, type IconFn } from "../_icon.ts";

/** Bytesize `CaretTop` icon. */
export const iconBytesizeCaretTop: IconFn = /* @__PURE__ */ icon(32, 2, ``, `viewBox="0 0 32 32" fill="none" stroke="currentcolor" stroke-linecap="round" stroke-linejoin="round"><path d="M30 22 L16 6 2 22 Z" /></svg>`);
