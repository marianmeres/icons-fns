import { icon, type IconFn } from "../_icon.ts";

/** Bytesize `Clock` icon. */
export const iconBytesizeClock: IconFn = /* @__PURE__ */ icon(32, 2, ``, `viewBox="0 0 32 32" fill="none" stroke="currentcolor" stroke-linecap="round" stroke-linejoin="round"><circle cx="16" cy="16" r="14" /><path d="M16 8 L16 16 20 20" /></svg>`);
