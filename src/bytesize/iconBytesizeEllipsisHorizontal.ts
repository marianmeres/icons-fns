/**
 * Bytesize `EllipsisHorizontal` icon.
 *
 * @module
 */
import { icon, type IconFn } from "../_icon.ts";

/** Bytesize `EllipsisHorizontal` icon. */
export const iconBytesizeEllipsisHorizontal: IconFn = /* @__PURE__ */ icon(32, 2, ``, `viewBox="0 0 32 32" fill="none" stroke="currentcolor" stroke-linecap="round" stroke-linejoin="round"><circle cx="7" cy="16" r="2" /><circle cx="16" cy="16" r="2" /><circle cx="25" cy="16" r="2" /></svg>`);
