import { icon, type IconFn } from "../_icon.ts";

/** Bytesize `Bookmark` icon. */
export const iconBytesizeBookmark: IconFn = /* @__PURE__ */ icon(32, 2, ``, `viewBox="0 0 32 32" fill="none" stroke="currentcolor" stroke-linecap="round" stroke-linejoin="round"><path d="M6 2 L26 2 26 30 16 20 6 30 Z" /></svg>`);
