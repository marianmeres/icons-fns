import { icon, type IconFn } from "../_icon.ts";

/** Bytesize `ArrowTop` icon. */
export const iconBytesizeArrowTop: IconFn = /* @__PURE__ */ icon(32, 2, ``, `viewBox="0 0 32 32" fill="none" stroke="currentcolor" stroke-linecap="round" stroke-linejoin="round"><path d="M6 10 L16 2 26 10 M16 2 L16 30" /></svg>`);
