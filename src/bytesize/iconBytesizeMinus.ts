import { icon, type IconFn } from "../_icon.ts";

/** Bytesize `Minus` icon. */
export const iconBytesizeMinus: IconFn = /* @__PURE__ */ icon(32, 2, ``, `viewBox="0 0 32 32" fill="none" stroke="currentcolor" stroke-linecap="round" stroke-linejoin="round"><path d="M2 16 L30 16" /></svg>`);
