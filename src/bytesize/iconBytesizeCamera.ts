/**
 * Bytesize `Camera` icon.
 *
 * @module
 */
import { icon, type IconFn } from "../_icon.ts";

/** Bytesize `Camera` icon. */
export const iconBytesizeCamera: IconFn = /* @__PURE__ */ icon(32, 2, ``, `viewBox="0 0 32 32" fill="none" stroke="currentcolor" stroke-linecap="round" stroke-linejoin="round"><path d="M2 8 L 9 8 12 4 20 4 23 8 30 8 30 26 2 26 Z" /><circle cx="16" cy="16" r="5" /></svg>`);
