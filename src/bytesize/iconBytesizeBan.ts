/**
 * Bytesize `Ban` icon.
 *
 * @module
 */
import { icon, type IconFn } from "../_icon.ts";

/** Bytesize `Ban` icon. */
export const iconBytesizeBan: IconFn = /* @__PURE__ */ icon(32, 2, ``, `viewBox="0 0 32 32" fill="none" stroke="currentcolor" stroke-linecap="round" stroke-linejoin="round"><circle cx="16" cy="16" r="14" /><path d="M6 6 L26 26" /></svg>`);
