import { icon, type IconFn } from "../_icon.ts";

/** Bytesize `ChevronLeft` icon. */
export const iconBytesizeChevronLeft: IconFn = /* @__PURE__ */ icon(32, 2, ``, `viewBox="0 0 32 32" fill="none" stroke="currentcolor" stroke-linecap="round" stroke-linejoin="round"><path d="M20 30 L8 16 20 2" /></svg>`);
