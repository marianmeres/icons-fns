/**
 * Bytesize `ArrowLeft` icon.
 *
 * @module
 */
import { icon, type IconFn } from "../_icon.ts";

/** Bytesize `ArrowLeft` icon. */
export const iconBytesizeArrowLeft: IconFn = /* @__PURE__ */ icon(32, 2, ``, `viewBox="0 0 32 32" fill="none" stroke="currentcolor" stroke-linecap="round" stroke-linejoin="round"><path d="M10 6 L2 16 10 26 M2 16 L30 16" /></svg>`);
