/**
 * Bytesize `ChevronTop` icon.
 *
 * @module
 */
import { icon, type IconFn } from "../_icon.ts";

/** Bytesize `ChevronTop` icon. */
export const iconBytesizeChevronTop: IconFn = /* @__PURE__ */ icon(32, 2, ``, `viewBox="0 0 32 32" fill="none" stroke="currentcolor" stroke-linecap="round" stroke-linejoin="round"><path d="M30 20 L16 8 2 20" /></svg>`);
