import { icon, type IconFn } from "../_icon.ts";

/** Bytesize `File` icon. */
export const iconBytesizeFile: IconFn = /* @__PURE__ */ icon(32, 2, ``, `viewBox="0 0 32 32" fill="none" stroke="currentcolor" stroke-linecap="round" stroke-linejoin="round"><path d="M6 2 L6 30 26 30 26 10 18 2 Z M18 2 L18 10 26 10" /></svg>`);
