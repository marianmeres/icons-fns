import { icon, type IconFn } from "../_icon.ts";

/** Bootstrap `CircleFill` icon. */
export const iconBsCircleFill: IconFn = /* @__PURE__ */ icon(16, null, ``, `fill="currentColor" viewBox="0 0 16 16"><circle cx="8" cy="8" r="8"/></svg>`);
