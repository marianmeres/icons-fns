import { icon, type IconFn } from "../../_icon.ts";

/** Phosphor bold `Dot` icon. */
export const iconPhBoldDot: IconFn = /* @__PURE__ */ icon(16, null, ``, `viewBox="0 0 256 256" fill="currentColor"><path d="M144,128a16,16,0,1,1-16-16A16,16,0,0,1,144,128Z"/></svg>`);
