/**
 * Phosphor thin `WifiNone` icon.
 *
 * @module
 */
import { icon, type IconFn } from "../../_icon.ts";

/** Phosphor thin `WifiNone` icon. */
export const iconPhThinWifiNone: IconFn = /* @__PURE__ */ icon(16, null, ``, `viewBox="0 0 256 256" fill="currentColor"><path d="M136,204a8,8,0,1,1-8-8A8,8,0,0,1,136,204Z"/></svg>`);
