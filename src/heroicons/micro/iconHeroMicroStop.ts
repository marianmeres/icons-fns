/**
 * Heroicons micro `Stop` icon.
 *
 * @module
 */
import { icon, type IconFn } from "../../_icon.ts";

/** Heroicons micro `Stop` icon. */
export const iconHeroMicroStop: IconFn = /* @__PURE__ */ icon(16, null, ``, `viewBox="0 0 16 16" fill="currentColor" aria-hidden="true" data-slot="icon"><rect x="3" y="3" rx="1.5"/></svg>`);
