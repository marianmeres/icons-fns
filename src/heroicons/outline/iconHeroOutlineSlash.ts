import { icon, type IconFn } from "../../_icon.ts";

/** Heroicons outline `Slash` icon. */
export const iconHeroOutlineSlash: IconFn = /* @__PURE__ */ icon(24, null, ``, `fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" aria-hidden="true" data-slot="icon"><path stroke-linecap="round" stroke-linejoin="round" d="m9 20.247 6-16.5"/></svg>`);
