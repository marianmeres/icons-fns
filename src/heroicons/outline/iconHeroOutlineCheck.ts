import { icon, type IconFn } from "../../_icon.ts";

/** Heroicons outline `Check` icon. */
export const iconHeroOutlineCheck: IconFn = /* @__PURE__ */ icon(24, null, ``, `fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" aria-hidden="true" data-slot="icon"><path stroke-linecap="round" stroke-linejoin="round" d="m4.5 12.75 6 6 9-13.5"/></svg>`);
