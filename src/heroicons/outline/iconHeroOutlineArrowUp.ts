/**
 * Heroicons outline `ArrowUp` icon.
 *
 * @module
 */
import { icon, type IconFn } from "../../_icon.ts";

/** Heroicons outline `ArrowUp` icon. */
export const iconHeroOutlineArrowUp: IconFn = /* @__PURE__ */ icon(24, null, ``, `fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" aria-hidden="true" data-slot="icon"><path stroke-linecap="round" stroke-linejoin="round" d="M4.5 10.5 12 3m0 0 7.5 7.5M12 3v18"/></svg>`);
