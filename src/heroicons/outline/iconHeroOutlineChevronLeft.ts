import { icon, type IconFn } from "../../_icon.ts";

/** Heroicons outline `ChevronLeft` icon. */
export const iconHeroOutlineChevronLeft: IconFn = /* @__PURE__ */ icon(24, null, ``, `fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" aria-hidden="true" data-slot="icon"><path stroke-linecap="round" stroke-linejoin="round" d="M15.75 19.5 8.25 12l7.5-7.5"/></svg>`);
