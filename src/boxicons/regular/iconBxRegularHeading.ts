/**
 * Boxicons regular `Heading` icon.
 *
 * @module
 */
import { icon, type IconFn } from "../../_icon.ts";

/** Boxicons regular `Heading` icon. */
export const iconBxRegularHeading: IconFn = /* @__PURE__ */ icon(24, null, ``, `viewBox="0 0 24 24"><path d="M18 20V4h-3v6H9V4H6v16h3v-7h6v7z"/></svg>`);
