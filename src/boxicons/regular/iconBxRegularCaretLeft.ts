/**
 * Boxicons regular `CaretLeft` icon.
 *
 * @module
 */
import { icon, type IconFn } from "../../_icon.ts";

/** Boxicons regular `CaretLeft` icon. */
export const iconBxRegularCaretLeft: IconFn = /* @__PURE__ */ icon(24, null, ``, `viewBox="0 0 24 24"><path d="M15 19V5l-8 7z"/></svg>`);
