import { icon, type IconFn } from "../../_icon.ts";

/** Boxicons regular `Signal1` icon. */
export const iconBxRegularSignal1: IconFn = /* @__PURE__ */ icon(24, null, ``, `viewBox="0 0 24 24"><path d="M3 16h2v5H3z"/></svg>`);
