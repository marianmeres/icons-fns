/**
 * Boxicons regular `Equalizer` icon.
 *
 * @module
 */
import { icon, type IconFn } from "../../_icon.ts";

/** Boxicons regular `Equalizer` icon. */
export const iconBxRegularEqualizer: IconFn = /* @__PURE__ */ icon(24, null, ``, `viewBox="0 0 24 24"><path d="M11 9h2v6h-2zm4-3h2v12h-2zM7 4h2v16H7zm12 7h2v2h-2zM3 10h2v4H3z"/></svg>`);
