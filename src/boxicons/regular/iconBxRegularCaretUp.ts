/**
 * Boxicons regular `CaretUp` icon.
 *
 * @module
 */
import { icon, type IconFn } from "../../_icon.ts";

/** Boxicons regular `CaretUp` icon. */
export const iconBxRegularCaretUp: IconFn = /* @__PURE__ */ icon(24, null, ``, `viewBox="0 0 24 24"><path d="M5 15h14l-7-8z"/></svg>`);
