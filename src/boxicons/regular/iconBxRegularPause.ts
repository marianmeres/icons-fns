import { icon, type IconFn } from "../../_icon.ts";

/** Boxicons regular `Pause` icon. */
export const iconBxRegularPause: IconFn = /* @__PURE__ */ icon(24, null, ``, `viewBox="0 0 24 24"><path d="M8 7h3v10H8zm5 0h3v10h-3z"/></svg>`);
