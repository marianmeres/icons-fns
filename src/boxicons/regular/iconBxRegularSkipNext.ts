/**
 * Boxicons regular `SkipNext` icon.
 *
 * @module
 */
import { icon, type IconFn } from "../../_icon.ts";

/** Boxicons regular `SkipNext` icon. */
export const iconBxRegularSkipNext: IconFn = /* @__PURE__ */ icon(24, null, ``, `viewBox="0 0 24 24"><path d="M7 7v10l7-5zm9 10V7h-2v10z"/></svg>`);
