/**
 * Boxicons regular `Signal2` icon.
 *
 * @module
 */
import { icon, type IconFn } from "../../_icon.ts";

/** Boxicons regular `Signal2` icon. */
export const iconBxRegularSignal2: IconFn = /* @__PURE__ */ icon(24, null, ``, `viewBox="0 0 24 24"><path d="M3 16h2v5H3zm4-3h2v8H7z"/></svg>`);
