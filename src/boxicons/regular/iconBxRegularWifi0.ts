/**
 * Boxicons regular `Wifi0` icon.
 *
 * @module
 */
import { icon, type IconFn } from "../../_icon.ts";

/** Boxicons regular `Wifi0` icon. */
export const iconBxRegularWifi0: IconFn = /* @__PURE__ */ icon(24, null, ``, `viewBox="0 0 24 24"><circle cx="12" cy="18" r="2"/></svg>`);
