/**
 * Boxicons regular `Minus` icon.
 *
 * @module
 */
import { icon, type IconFn } from "../../_icon.ts";

/** Boxicons regular `Minus` icon. */
export const iconBxRegularMinus: IconFn = /* @__PURE__ */ icon(24, null, ``, `viewBox="0 0 24 24"><path d="M5 11h14v2H5z"/></svg>`);
