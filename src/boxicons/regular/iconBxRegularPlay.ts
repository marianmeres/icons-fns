/**
 * Boxicons regular `Play` icon.
 *
 * @module
 */
import { icon, type IconFn } from "../../_icon.ts";

/** Boxicons regular `Play` icon. */
export const iconBxRegularPlay: IconFn = /* @__PURE__ */ icon(24, null, ``, `viewBox="0 0 24 24"><path d="M7 6v12l10-6z"/></svg>`);
