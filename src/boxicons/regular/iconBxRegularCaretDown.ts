import { icon, type IconFn } from "../../_icon.ts";

/** Boxicons regular `CaretDown` icon. */
export const iconBxRegularCaretDown: IconFn = /* @__PURE__ */ icon(24, null, ``, `viewBox="0 0 24 24"><path d="m11.998 17 7-8h-14z"/></svg>`);
