/**
 * Boxicons regular `Stop` icon.
 *
 * @module
 */
import { icon, type IconFn } from "../../_icon.ts";

/** Boxicons regular `Stop` icon. */
export const iconBxRegularStop: IconFn = /* @__PURE__ */ icon(24, null, ``, `viewBox="0 0 24 24"><path d="M7 7h10v10H7z"/></svg>`);
