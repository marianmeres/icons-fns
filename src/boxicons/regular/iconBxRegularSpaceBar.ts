/**
 * Boxicons regular `SpaceBar` icon.
 *
 * @module
 */
import { icon, type IconFn } from "../../_icon.ts";

/** Boxicons regular `SpaceBar` icon. */
export const iconBxRegularSpaceBar: IconFn = /* @__PURE__ */ icon(24, null, ``, `viewBox="0 0 24 24"><path d="M17 13H7V9H5v6h14V9h-2z"/></svg>`);
