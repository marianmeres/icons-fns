/**
 * Boxicons solid `ToTop` icon.
 *
 * @module
 */
import { icon, type IconFn } from "../../_icon.ts";

/** Boxicons solid `ToTop` icon. */
export const iconBxSolidToTop: IconFn = /* @__PURE__ */ icon(24, null, ``, `viewBox="0 0 24 24"><path d="M5 15h4v6h6v-6h4l-7-8zM4 3h16v2H4z"/></svg>`);
