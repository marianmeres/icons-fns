/**
 * Boxicons solid `ArrowFromTop` icon.
 *
 * @module
 */
import { icon, type IconFn } from "../../_icon.ts";

/** Boxicons solid `ArrowFromTop` icon. */
export const iconBxSolidArrowFromTop: IconFn = /* @__PURE__ */ icon(24, null, ``, `viewBox="0 0 24 24"><path d="M6 4h12v2H6zm5 4v6H6l6 6 6-6h-5V8z"/></svg>`);
