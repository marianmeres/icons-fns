/**
 * Boxicons solid `CircleQuarter` icon.
 *
 * @module
 */
import { icon, type IconFn } from "../../_icon.ts";

/** Boxicons solid `CircleQuarter` icon. */
export const iconBxSolidCircleQuarter: IconFn = /* @__PURE__ */ icon(24, null, ``, `viewBox="0 0 24 24"><path d="M12 2h-1v11h11v-1A10 10 0 0 0 12 2z"/></svg>`);
