/**
 * Boxicons solid `CircleHalf` icon.
 *
 * @module
 */
import { icon, type IconFn } from "../../_icon.ts";

/** Boxicons solid `CircleHalf` icon. */
export const iconBxSolidCircleHalf: IconFn = /* @__PURE__ */ icon(24, null, ``, `viewBox="0 0 24 24"><path d="M12 2h-1v20h1a10 10 0 0 0 0-20z"/></svg>`);
