/**
 * Feather `ChevronDown` icon.
 *
 * @module
 */
import { icon, type IconFn } from "../_icon.ts";

/** Feather `ChevronDown` icon. */
export const iconFeatherChevronDown: IconFn = /* @__PURE__ */ icon(24, 2, ``, `viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9"></polyline></svg>`);
