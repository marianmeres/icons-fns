/**
 * Feather `CornerDownLeft` icon.
 *
 * @module
 */
import { icon, type IconFn } from "../_icon.ts";

/** Feather `CornerDownLeft` icon. */
export const iconFeatherCornerDownLeft: IconFn = /* @__PURE__ */ icon(24, 2, ``, `viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round"><polyline points="9 10 4 15 9 20"></polyline><path d="M20 4v7a4 4 0 0 1-4 4H4"></path></svg>`);
