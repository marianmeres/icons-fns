/**
 * Feather `AtSign` icon.
 *
 * @module
 */
import { icon, type IconFn } from "../_icon.ts";

/** Feather `AtSign` icon. */
export const iconFeatherAtSign: IconFn = /* @__PURE__ */ icon(24, 2, ``, `viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="4"></circle><path d="M16 8v5a3 3 0 0 0 6 0v-1a10 10 0 1 0-3.92 7.94"></path></svg>`);
