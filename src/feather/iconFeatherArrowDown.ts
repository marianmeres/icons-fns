/**
 * Feather `ArrowDown` icon.
 *
 * @module
 */
import { icon, type IconFn } from "../_icon.ts";

/** Feather `ArrowDown` icon. */
export const iconFeatherArrowDown: IconFn = /* @__PURE__ */ icon(24, 2, ``, `viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round"><line x1="12" y1="5" x2="12" y2="19"></line><polyline points="19 12 12 19 5 12"></polyline></svg>`);
