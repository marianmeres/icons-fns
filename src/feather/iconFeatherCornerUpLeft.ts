/**
 * Feather `CornerUpLeft` icon.
 *
 * @module
 */
import { icon, type IconFn } from "../_icon.ts";

/** Feather `CornerUpLeft` icon. */
export const iconFeatherCornerUpLeft: IconFn = /* @__PURE__ */ icon(24, 2, ``, `viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round"><polyline points="9 14 4 9 9 4"></polyline><path d="M20 20v-7a4 4 0 0 0-4-4H4"></path></svg>`);
