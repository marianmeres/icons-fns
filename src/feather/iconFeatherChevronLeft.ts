import { icon, type IconFn } from "../_icon.ts";

/** Feather `ChevronLeft` icon. */
export const iconFeatherChevronLeft: IconFn = /* @__PURE__ */ icon(24, 2, ``, `viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round"><polyline points="15 18 9 12 15 6"></polyline></svg>`);
