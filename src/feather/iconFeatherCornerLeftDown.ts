/**
 * Feather `CornerLeftDown` icon.
 *
 * @module
 */
import { icon, type IconFn } from "../_icon.ts";

/** Feather `CornerLeftDown` icon. */
export const iconFeatherCornerLeftDown: IconFn = /* @__PURE__ */ icon(24, 2, ``, `viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round"><polyline points="14 15 9 20 4 15"></polyline><path d="M20 4h-7a4 4 0 0 0-4 4v12"></path></svg>`);
