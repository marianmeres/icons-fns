/**
 * Feather `CornerLeftUp` icon.
 *
 * @module
 */
import { icon, type IconFn } from "../_icon.ts";

/** Feather `CornerLeftUp` icon. */
export const iconFeatherCornerLeftUp: IconFn = /* @__PURE__ */ icon(24, 2, ``, `viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round"><polyline points="14 9 9 4 4 9"></polyline><path d="M20 20h-7a4 4 0 0 1-4-4V4"></path></svg>`);
