import { icon, type IconFn } from "../_icon.ts";

/** Feather `Minus` icon. */
export const iconFeatherMinus: IconFn = /* @__PURE__ */ icon(24, 2, ``, `viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round"><line x1="5" y1="12" x2="19" y2="12"></line></svg>`);
