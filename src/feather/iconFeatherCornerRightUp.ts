import { icon, type IconFn } from "../_icon.ts";

/** Feather `CornerRightUp` icon. */
export const iconFeatherCornerRightUp: IconFn = /* @__PURE__ */ icon(24, 2, ``, `viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round"><polyline points="10 9 15 4 20 9"></polyline><path d="M4 20h7a4 4 0 0 0 4-4V4"></path></svg>`);
