/**
 * Feather `TrendingDown` icon.
 *
 * @module
 */
import { icon, type IconFn } from "../_icon.ts";

/** Feather `TrendingDown` icon. */
export const iconFeatherTrendingDown: IconFn = /* @__PURE__ */ icon(24, 2, ``, `viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round"><polyline points="23 18 13.5 8.5 8.5 13.5 1 6"></polyline><polyline points="17 18 23 18 23 12"></polyline></svg>`);
