/**
 * Feather `MousePointer` icon.
 *
 * @module
 */
import { icon, type IconFn } from "../_icon.ts";

/** Feather `MousePointer` icon. */
export const iconFeatherMousePointer: IconFn = /* @__PURE__ */ icon(24, 2, ``, `viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round"><path d="M3 3l7.07 16.97 2.51-7.39 7.39-2.51L3 3z"></path><path d="M13 13l6 6"></path></svg>`);
