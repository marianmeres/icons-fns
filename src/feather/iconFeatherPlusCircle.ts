/**
 * Feather `PlusCircle` icon.
 *
 * @module
 */
import { icon, type IconFn } from "../_icon.ts";

/** Feather `PlusCircle` icon. */
export const iconFeatherPlusCircle: IconFn = /* @__PURE__ */ icon(24, 2, ``, `viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"></circle><line x1="12" y1="8" x2="12" y2="16"></line><line x1="8" y1="12" x2="16" y2="12"></line></svg>`);
