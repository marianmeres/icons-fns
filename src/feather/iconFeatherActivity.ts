import { icon, type IconFn } from "../_icon.ts";

/** Feather `Activity` icon. */
export const iconFeatherActivity: IconFn = /* @__PURE__ */ icon(24, 2, ``, `viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round"><polyline points="22 12 18 12 15 21 9 3 6 12 2 12"></polyline></svg>`);
