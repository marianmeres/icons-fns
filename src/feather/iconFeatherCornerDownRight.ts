import { icon, type IconFn } from "../_icon.ts";

/** Feather `CornerDownRight` icon. */
export const iconFeatherCornerDownRight: IconFn = /* @__PURE__ */ icon(24, 2, ``, `viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round"><polyline points="15 10 20 15 15 20"></polyline><path d="M4 4v7a4 4 0 0 0 4 4h12"></path></svg>`);
