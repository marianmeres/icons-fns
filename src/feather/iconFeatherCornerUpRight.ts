/**
 * Feather `CornerUpRight` icon.
 *
 * @module
 */
import { icon, type IconFn } from "../_icon.ts";

/** Feather `CornerUpRight` icon. */
export const iconFeatherCornerUpRight: IconFn = /* @__PURE__ */ icon(24, 2, ``, `viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round"><polyline points="15 14 20 9 15 4"></polyline><path d="M4 20v-7a4 4 0 0 1 4-4h12"></path></svg>`);
