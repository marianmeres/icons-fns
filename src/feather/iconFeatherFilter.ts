/**
 * Feather `Filter` icon.
 *
 * @module
 */
import { icon, type IconFn } from "../_icon.ts";

/** Feather `Filter` icon. */
export const iconFeatherFilter: IconFn = /* @__PURE__ */ icon(24, 2, ``, `viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round"><polygon points="22 3 2 3 10 12.46 10 19 14 21 14 12.46 22 3"></polygon></svg>`);
