/**
 * Feather `ChevronUp` icon.
 *
 * @module
 */
import { icon, type IconFn } from "../_icon.ts";

/** Feather `ChevronUp` icon. */
export const iconFeatherChevronUp: IconFn = /* @__PURE__ */ icon(24, 2, ``, `viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round"><polyline points="18 15 12 9 6 15"></polyline></svg>`);
