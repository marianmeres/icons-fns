/**
 * Feather `ArrowUpLeft` icon.
 *
 * @module
 */
import { icon, type IconFn } from "../_icon.ts";

/** Feather `ArrowUpLeft` icon. */
export const iconFeatherArrowUpLeft: IconFn = /* @__PURE__ */ icon(24, 2, ``, `viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round"><line x1="17" y1="17" x2="7" y2="7"></line><polyline points="7 17 7 7 17 7"></polyline></svg>`);
