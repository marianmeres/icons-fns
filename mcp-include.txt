Icon SVGs from eight providers (Bootstrap, Boxicons, Bytesize, Feather, Font
Awesome, Heroicons, Lucide, Phosphor) wrapped as ~19k individually importable
functions returning an SVG string. Includes name search, so an agent can find
the exact export name and import specifier for an icon before writing code.
